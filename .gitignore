node_modules
dist
coverage
.turbo
*.tsbuildinfo

# Playwright
test-results
playwright-report
blob-report
playwright/.cache

# Vendored Stockfish WASM, copied from the stockfish package at build time.
apps/web/public/engine/

# Deno lockfile generated when running edge functions locally (versions are
# pinned inline via npm: specifiers).
supabase/functions/deno.lock
*.local
.DS_Store
.wrangler

# Secrets — never commit. Use .env.example for the template.
.env
.env.*
!.env.example

notes.md

.env
